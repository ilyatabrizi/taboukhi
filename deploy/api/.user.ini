; Read by PHP-FPM / LSAPI for this folder (php_value in .htaccess breaks FPM, so it lives here).
; Changes take up to five minutes to apply (user_ini.cache_ttl).
upload_max_filesize = 12M
post_max_size = 14M
max_file_uploads = 2
max_execution_time = 60
memory_limit = 256M
display_errors = Off
